source "https://rubygems.org"
gemspec

group :jekyll_plugins do
  # gem "github-pages" # push到github時要反註解, 在windows開發時要註解
  gem "jekyll-admin"
  gem 'tzinfo-data', platforms: [:mingw, :mswin, :x64_mingw] # 只在windows作用
end